#include <bits/stdc++.h>

using namespace std;


int main() {
    int t;
    cin >> t;

    for (int a = 0; a < t; a++) {
        int n, m;
        cin >> n >> m;
        vector<int> paint(m);
        for (int i = 0; i < m; i++) {
            cin >> paint[i];
        }
    }
}