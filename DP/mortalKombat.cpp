#include <bits/stdc++.h>


using namespace std;

int main() {
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        int N;
        cin >> N;
        vector<int> bosses(N);
        for (int j = 0; j < N; j++) {
            cin >> bosses[j];
        }

        vector<int> dp(N + 1, 0);
        dp[0] = 0;
        for (int j = 0; j <= N; j++) {
            
        }
    }
}